# R-Type

A touhou game but with aliens

### Dev guidelines

`include/global` are elements visible to both server and client
`include/server` are elements visible only to the server
`include/client` are elements visible only to the client

`CODEOWNERS` help ever keep track of who is responsible for which part of the project.  
Please do the necessary so that it's keep up to date with your latest changes.
